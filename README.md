# curly-guacamole-MATLAB
Write a code that plots the rows of a matrix (rows) one by one. after plotting eah vector, if you clicked the plot with the mouse it should store that row (array) in a new matrix.
